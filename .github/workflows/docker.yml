name: Docker

on:
  push:
    branches: [main]
    paths-ignore:
      - "**.md"
      - ".gitignore"
      - ".dockerignore"
  pull_request:
    branches: [main]

env:
  IMAGE_NAME: legajo-digital

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: ${{ env.IMAGE_NAME }}:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Start container
        run: |
          docker run -d \
            --name legajo-digital \
            -p 3000:3000 \
            -v ${{ github.workspace }}/datos_locales:/app/datos_locales \
            ${{ env.IMAGE_NAME }}:test

      - name: Wait for health check
        run: |
          echo "Esperando que el servidor responda..."
          for i in $(seq 1 15); do
            if curl -s http://localhost:3000/ > /dev/null 2>&1; then
              echo "Servidor listo!"
              break
            fi
            echo "  Intento $i/15..."
            sleep 2
          done

      - name: Smoke test HTML
        run: |
          echo "=== GET / ==="
          HTTP_CODE=$(curl -s -o /tmp/index.html -w "%{http_code}" http://localhost:3000/)
          echo "Status: $HTTP_CODE"
          test "$HTTP_CODE" -eq 200 || exit 1
          grep -q "Legajo Digital" /tmp/index.html || exit 1
          echo "HTML OK"

      - name: Smoke test API
        run: |
          echo "=== GET /agentes ==="
          AGENTES=$(curl -s http://localhost:3000/agentes)
          echo "$AGENTES" | python3 -c "import sys,json; d=json.load(sys.stdin); assert isinstance(d.get('agentes'),list); print(f'{len(d[\"agentes\"])} docentes')"
          echo "API OK"

      - name: Stop container
        run: docker stop legajo-digital

      - name: Upload logs on failure
        if: failure()
        run: docker logs legajo-digital 2>&1 || true

  push-to-ghcr:
    name: Push to GHCR
    needs: build-and-test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
